/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // For Firebase Hosting: Enable static export
  // Comment this out for local development with API routes
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
}

module.exports = nextConfig
